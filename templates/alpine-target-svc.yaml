{{- if .Values.target.ssh.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: ssh-target
spec:
  clusterIP: None
  selector:
    app: ssh-target
  ports:
  {{- range .Values.target.ssh.ports }}
  - name: {{ .name }}
    port: {{ .port }}
    targetPort: {{ .port }}
  {{- end }}
{{- end }}